// TODO: FIX PROPS TYPES, DEPENDING FROM WHERE THE DIRECTORY STATUS INFORMATION COMES
import { IndexDirectory } from '../../../../../../services/printer-air/types';
export interface StatusCellProps {
  directory: IndexDirectory;
}

export interface StatusCellContainerProps {
  directory: IndexDirectory;
}

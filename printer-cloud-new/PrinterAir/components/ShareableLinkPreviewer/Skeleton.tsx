import * as React from 'react';
import { Skeleton } from 'printer-ui';

const ShareableLinkPreviewerSkeleton = () => {
  return <Skeleton className="w-[80vw] h-[80vh]" w={1} h={1} />;
};

export default ShareableLinkPreviewerSkeleton;

import * as React from 'react';
import { Skeleton } from 'printer-ui';

const ShareableLinkPreviewerContentSkeleton = ({}) => {
  return <Skeleton className="w-[75vw] h-[75vh]" w={1} h={1} />;
};

export default ShareableLinkPreviewerContentSkeleton;

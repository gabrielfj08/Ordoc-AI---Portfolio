import { procedureTemplateSource } from '../../services/printer-flow/types';

export const SourceProcedureTemplate: Record<string, procedureTemplateSource> =
  {
    internal: 'internal',
    external: 'external',
    internal_external: 'internal_external',
  };
